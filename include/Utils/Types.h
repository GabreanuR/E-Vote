#ifndef TYPES_H
#define TYPES_H

enum class UserType {
    voter,
    admin
};

enum class ElectionLevel {
    national,
    regional,
    municipal,
    local,
    nonGovernment
};

#endif // TYPES_H
